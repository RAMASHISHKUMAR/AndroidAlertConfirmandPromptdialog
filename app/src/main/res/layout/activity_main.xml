<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="15dp"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/btn1AlertDialog"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Alert dialog"
        android:onClick="ClickAnyButton"
        android:layout_marginTop="200dp"/>
    <Button
        android:id="@+id/btn2ConformDialog"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Confirm dialog"
        android:onClick="ClickAnyButton"
        android:layout_below="@+id/btn1AlertDialog"
        android:layout_marginTop="20dp"/>
    <Button
        android:id="@+id/btn3PromptDialog"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Prompt dialog"
        android:onClick="ClickAnyButton"
        android:layout_below="@+id/btn2ConformDialog"
        android:layout_marginTop="20dp"/>



</RelativeLayout>